Bayside staging site — original navy header, embedded original shell logo, and approved blue-scrubs clinical hero.

Copy ALL contents of this folder into baysidefees/staging (index.html must be directly in staging). Run: cd /path/to/baysidefees/staging && python3 -m http.server 8002. Open http://localhost:8002.

Navigation labels preserved from original single-page site; links point to corresponding pages in this prototype. GitHub Pages has no server-side password protection. Do not use this static prototype to collect patient health information.
